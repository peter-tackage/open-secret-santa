<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  android:installLocation="preferExternal"
  android:versionCode="6"
  android:versionName="1.4.1"
  package="com.moac.android.opensecretsanta">

  <uses-permission android:name="android.permission.READ_CONTACTS" />
  <uses-permission android:name="android.permission.SEND_SMS" />

  <uses-sdk
    android:minSdkVersion="14"
    android:targetSdkVersion="17" />

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.GET_ACCOUNTS" />
  <uses-permission android:name="android.permission.USE_CREDENTIALS" />

  <application
    android:debuggable="true"
    android:icon="@drawable/icon"
    android:label="@string/app_name"
    android:name=".OpenSecretSantaApplication"
    android:theme="@style/AppTheme">

    <activity android:name=".activity.MembersListActivity" />
    <activity android:name=".activity.AssignmentSharerActivity" />
    <activity android:name=".activity.AllPreferencesActivity" />
    <activity
      android:name=".activity.NewDrawActivity"
      android:launchMode="singleTop">   <!-- For search -->
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.SEARCH" />
      </intent-filter>
      <meta-data android:name="android.app.searchable"
        android:resource="@xml/searchable"/>
    </activity>

    <provider
      android:authorities="com.moac.android.opensecretsanta.membersuggestions"
      android:name=".content.MemberSuggestionProvider" >
    </provider>

  </application>

</manifest>