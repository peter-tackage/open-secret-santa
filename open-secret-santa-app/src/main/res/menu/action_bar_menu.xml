<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Non-CAB Action Bar items -->
    <item
        android:id="@+id/menu_item_draw"
        android:showAsAction="ifRoom"
        android:icon="@drawable/ic_action_cc_bcc"
        android:title="@string/menu_assign" />

    <item
        android:id="@+id/menu_item_notify_group"
        android:showAsAction="ifRoom"
        android:icon="@drawable/ic_menu_notify"
        android:title="@string/menu_notify_group" />

</menu>